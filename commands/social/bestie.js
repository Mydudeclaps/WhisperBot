const { buildTargetCommand } = require("../../social-engine/engine/SocialCommandRunner");

module.exports = buildTargetCommand({
    name: "bestie",
    description: "💗 Declare someone your official bestie!"
});
