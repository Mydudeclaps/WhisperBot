const { buildTargetContextCommand } = require("../../social-engine/engine/SocialCommandRunner");

module.exports = buildTargetContextCommand({
    name: "throwpotato",
    displayName: "Throw Potato"
});
