module.exports = {

    // ============================================================
    // ADMIN ROLE IDs
    // ============================================================
    // These are Discord role IDs that can use admin commands.
    // Users with Administrator permission ALWAYS have access.
    // Add as many role IDs as you want (separate with commas).
    // ============================================================
    
    ADMIN_ROLE_IDS: [
        '1503015617207472219',  // ← Developer Tier VI
        '1489396669576380496', // ← Admin Top Tier
        '1512197736827584713', // ← Legend this is my role
    ],

    // ============================================================
    // ADMIN LOG CHANNEL ID
    // ============================================================
    // Where admin actions get logged (coins, inventory, resets, etc.)
    // Leave empty ("") to disable logging (no errors, just silent).
    // ============================================================
    
    ADMIN_LOG_CHANNEL_ID: "1511183676572962967"  // ← mod-log channel

};